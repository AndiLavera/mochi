defaults : &defaults
  host: localhost
  adapter: postgres
  user: mochi
  password: mochi
  migration_files_path: db/migrations # this is the default location for all migrations

test:
  db: mochi_test
  <<: *defaults