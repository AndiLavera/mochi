dist: bionic
sudo: required
language: crystal

services:
  - postgresql
  - redis-server

cache:
  directories:
    - bin
    - lib
    - .shards

env:
  - TEST_SUITE=./spec/mochi

script:
  - bin/ameba
  - crystal spec/sam.cr -- db:migrate
  - crystal tool format --check
  - crystal spec
